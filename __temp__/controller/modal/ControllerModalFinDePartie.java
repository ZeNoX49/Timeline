package app.controller.modal;

import app.Main;
import app.util.GameManager;
import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.scene.control.Label;
import javafx.stage.Stage;

public class ControllerModalFinDePartie {
    // GameManager gameManager = GameManager.getInstance();

    // @FXML private Label nomWin;
    // @FXML private Label scoreWin;
    
    // public void initialize() {
    //     nomWin.setText("Félicitations, " + gameManager.getNomWin() + " !");
    //     if(gameManager.getNbJoueur() == 1) {
    //         scoreWin.setText("Votre score : " + gameManager.getScore());
    //     } else {
    //         scoreWin.setText("""
    //             Score de %s : %d\n
    //             Score de %s : %d
    //             """.formatted(gameManager.getNomJ1(), gameManager.getScore(), gameManager.getNomJ1(), gameManager.getScore())
    //         );
    //     }
    // }

    // @FXML
    // void onConfirmQuit(ActionEvent event) {
    // 	Stage stage = (Stage) nomWin.getScene().getWindow(); 
    //     stage.close();

    // 	Main.switchPage("accueil.fxml");
    // }

}
